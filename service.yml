name: confluent-sql
lang: unknown
lang_version: unknown
git:
  enable: true
codeowners:
  enable: true
renovatebot:
  enable: true
  automerge:
    enable: true
semaphore:
  enable: true
  status_level: block
  pipeline_enable: false
  managed_sections:
    - version
    - name
    - agent
    - global_job_config
make:
  enable: false
code_artifact:
  enable: true
  package_paths:
    - pypi-internal/pypi//confluent-sql*